# Genetic-Simulated-Annealing-CNF-Solver
A Python-based solution for the Boolean Satisfiability (SAT) problem. This project implements and compares two classical metaheuristic search algorithms: the Genetic Algorithm (GA) and Simulated Annealing (SA), using CNF files as input.
